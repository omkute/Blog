// Blog types 
export type BlogPostProps = {
    id:string,
    title: string;
    description: string;
    author: string;
    date: string;
    views: number;
    imageUrl: string;
}

//Hashtag types

export type HashTagProps = {
    tag:string
}